import 'package:flutter/material.dart';

class LabResultsView extends StatelessWidget {
  const LabResultsView({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(body: SafeArea(child: Text('Labs')));
  }
}

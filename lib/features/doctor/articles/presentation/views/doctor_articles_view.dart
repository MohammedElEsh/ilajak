import 'package:flutter/material.dart';

class DoctorArticlesView extends StatelessWidget {
  const DoctorArticlesView({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Doctor Articles'),
    );
  }
}
